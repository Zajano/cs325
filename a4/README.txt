in the Zack_Jaffe_A4 directory, enter 'make' into the command line. After the makefile has run, enter 'scheduler' to run the executable. If the file is successful, the selected activities should be printed to the console.

if the executable doesn't run, you may need to enter "./scheduler" in the command line to run it. 
